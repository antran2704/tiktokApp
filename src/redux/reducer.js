const initState = {
  listVideo: [],
  selectVideo: {},
  listVideoUser: [],
};

const rootReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default rootReducer
